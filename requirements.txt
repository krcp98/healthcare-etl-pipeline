# Minimal requirements.txt for AWS Glue Production
# Most dependencies are pre-installed in Glue runtime

# Only add external libraries not available in Glue
# Currently no additional dependencies needed for basic ETL pipeline

# For local development only (comment out for Glue deployment):
# pyspark>=3.3.0
# boto3>=1.26.137
# pandas>=1.5.0

# Add custom libraries here if needed:
# great-expectations>=0.15.0  # For advanced data validation
# requests>=2.28.0            # For API integrations